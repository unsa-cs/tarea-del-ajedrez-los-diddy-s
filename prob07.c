#include "chess.h"
#include "figures.h"
#include <stdlib.h>

char **posicion(int pos, char **pieza) {
  char **fila = 0;

  for (int i = 0; i < 4; i++) {
    for (int j = 0; j < 4; j++) {
      if (i + j % 2 == 0) {
        whiteSquare;
      } else {
        reverse(whiteSquare);
      }
    }
  }
}
void display() {

  interpreter(posicion(,)
}
